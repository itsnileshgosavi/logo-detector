export const metadata = {
    title: "Sign UP: Logo Detectot",
    description: "This app can identify any brand by its logo",
  };
  
  export default function SignUpLayout({ children }) {
    return (
      <div>
        {children}
      </div>
    );
  }
  